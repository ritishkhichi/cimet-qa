# Copy values into .env (do not commit .env)

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cimet_qa"

# Only used for Option A (audio → transcript). Leave empty to use Option B only.
DEEPGRAM_API_KEY=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"
