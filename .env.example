# Copy into .env — never commit .env

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cimet_qa"

# Phase 1 — Option A audio STT (optional)
DEEPGRAM_API_KEY=""

# Scoring + PDF→utterances (Groq — https://console.groq.com)
GROQ_API_KEY=""
# Optional (default: llama-3.3-70b-versatile)
# GROQ_MODEL="llama-3.3-70b-versatile"

# Phase 2 — Auth.js
AUTH_SECRET=""

# Critical HELD alerts — auto only when gate = HELD (skipped on all-pass)
ALERT_TO_EMAIL="ritish143khichi@gmail.com"
# Preferred (reliable): Gmail App Password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=ritish143khichi@gmail.com
# SMTP_PASS=
# Or Resend: RESEND_API_KEY=
# Fallback: FormSubmit (confirm activation email once)
